set -e

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.8.3)
export PATH=$GOROOT/bin:$PATH
export GOPATH=$PWD/loggregator-capacity-planning-release

go install code.cloudfoundry.org/event_counter
cp -a $PWD/loggregator-capacity-planning-release/bin/event_counter ${BOSH_INSTALL_TARGET}

